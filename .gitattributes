# Normalize line endings to LF in the repository (Biome/CI expect LF).
* text=auto eol=lf

# Treat binary assets as binary (no EOL normalization, no diffs).
*.jpg binary
*.jpeg binary
*.png binary
*.avif binary
*.webp binary
*.woff binary
*.woff2 binary
*.ico binary
